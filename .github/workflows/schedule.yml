name: Scheduled Workflow

on:
  schedule:
    - cron: '45 6 * * *'

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "This workflow runs every day at midnight UTC"
