name: PR Check

on:
  workflow_dispatch: 
 

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Show PR branch
        run: |
          echo "PR check running for branch: ${{ github.head_ref }}"
